import type { ServerOptions } from '#type:server';

/**
 * Configuration options for the server.
 */
export const serverOptions: ServerOptions =
{
  /**
   * The port number on which the server will listen for incoming requests (default: `81`).
   */
  port: 81
};