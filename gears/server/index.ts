import { createServer } from './cogs/server';

/**
 * Re-exports the `createServer` function to create a more
 * convenient and organized API for other parts of the application.
 */
export { createServer };